=LET(
ref,TRIM($E4),
fmt,UPPER(TRIM($T4)),
lng,$W4,
rule,$U4,
colName,"P1_"&SUBSTITUTE(SUBSTITUTE(ref,".","_")," ","_"),
cutTxt,IFERROR(LEFT(rule,SEARCH("cimale",rule)-1),""),
cutTxt2,IFERROR(TRIM(LEFT(TRIM(cutTxt),LEN(TRIM(cutTxt))-2)),""),
nbDec,IFERROR(VALUE(TRIM(RIGHT(SUBSTITUTE(cutTxt2," ",REPT(" ",50)),50))),0),
sgn,IF(ISNUMBER(SEARCH("signe",rule)),1,0),
sep,IF(ISNUMBER(SEARCH("parateur",rule)),1,0),
colType,
IF(fmt="DATE",
"DATE",
IF(fmt="ALPHA",
"VARCHAR2("&lng&")",
IF(fmt="NUM",
"NUMBER("&(lng-sgn-sep)&IF(nbDec>0,","&nbDec,"")&")",
"VARCHAR2("&lng&")"
)
)
),
IF(
LEFT(ref,3)<>"P1 ",
"",
"    "&colName&"  "&colType&" ,  -- "&ref&" "&$F4
)
)




=LET(ref,TRIM($E4),fmt,UPPER(TRIM($T4)),lng,$W4,rule,$U4,num,SUBSTITUTE(SUBSTITUTE(SUBSTITUTE(ref,"(P1)",""),"P1","")," ",""),isH,ISNUMBER(SEARCH("(P1)",ref)),col,IF(isH,"P1_H_","P1_")&SUBSTITUTE(num,".","_"),cut,IFERROR(LEFT(rule,SEARCH("cimale",rule)-1),""),cut2,IFERROR(TRIM(LEFT(TRIM(cut),LEN(TRIM(cut))-2)),""),dec,IFERROR(VALUE(TRIM(RIGHT(SUBSTITUTE(cut2," ",REPT(" ",50)),50))),0),sgn,IF(ISNUMBER(SEARCH("signe",rule)),1,0),sep,IF(ISNUMBER(SEARCH("parateur",rule)),1,0),typ,IF(fmt="DATE","DATE",IF(fmt="ALPHA","VARCHAR2("&lng&")",IF(fmt="NUM","NUMBER("&(lng-sgn-sep)&IF(dec>0,","&dec,"")&")","VARCHAR2("&lng&")"))),IF($A4<>"P1","","    "&col&"  "&typ&" ,  -- "&$E4&" "&$F4))
